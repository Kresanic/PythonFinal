import game.krep09_kresanic as at
at.self_test()

""" 
    if arguments[1] in can_cook:
        
        # Cooking Quesadilla

        if meal == can_cook[0]:
            if not conditions[f'has.{meal}']:
                needed_for_quesadilla = ['Bravčové', 'Tortilla', 'Paradajky']
                if checks_for_needed_foods(needed_for_quesadilla):
                    remove_items(needed_for_quesadilla)
                    creates_meal(meal)
                    conditions[f'has.{meal}'] = True
                    return NS_1 + meal
                else:
                    return NS1_WRONG_ARGb
            else:
                return NS1_WrongCond

        # Cooking Bageta
        if meal == can_cook[1]:
            if not conditions[f'has.{meal}']:
                needed_for_bageta = ['Pečivo', 'Šunka', 'Syr']
                if checks_for_needed_foods(needed_for_bageta):
                    remove_items(needed_for_bageta)
                    creates_meal(meal)
                    conditions[f'has.{meal}'] = True
                    return NS_1 + meal
                else:
                    return NS1_WRONG_ARGb
            else:
                return NS1_WrongCond

        # Cooking Rezeň
        if meal == can_cook[2]:
            if not conditions[f'has.{meal}']:
                needed_for_rezen = ['Kuracie', 'Strúhanka', 'Vajíčka']
                if checks_for_needed_foods(needed_for_rezen):
                    remove_items(needed_for_rezen)
                    creates_meal(meal)
                    conditions[f'has.{meal}'] = True
                    return NS_1 + meal
                else:
                    return NS1_WRONG_ARGb
            else:
                return NS1_WrongCond

        # Cooking Sviečková
        if meal == can_cook[3]:
            if not conditions[f'has.{meal}']:
                needed_for_svieckova = ['Hovädzie', 'Knedlík', 'Omáčka']
                if checks_for_needed_foods(needed_for_svieckova):
                    remove_items(needed_for_svieckova)
                    creates_meal(meal)
                    conditions[f'has.{meal}'] = True
                    return NS_1 + meal
                else:
                    return NS1_WRONG_ARGb
            else:
                return NS1_WrongCond

"""

